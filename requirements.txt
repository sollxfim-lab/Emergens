# Web framework
Flask==3.1.3
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.0
blinker==1.9.0

# Production WSGI
gunicorn==26.1.0
asgiref==3.9.1

# Auth & crypto
PyJWT==2.13.0
cryptography==48.0.1
passlib==1.7.4

# HTTP & scraping
requests==2.34.2
urllib3==2.8.0
certifi==2026.7.22
chardet==5.2.0
charset-normalizer==3.5.1
idna==3.19.0
httpx==0.28.1
cloudscraper==1.2.71
yarl==1.20.1
beautifulsoup4==4.15.0
lxml==6.0.1
soupsieve==2.8.4

# Cloudflare bypass — curl_cffi TLS fingerprint impersonation
# Fallback range if 0.9.0 unavailable: curl_cffi>=0.7,<0.10
curl_cffi==0.9.0

# Config parsers (fallbacks for Python < 3.11; no-op on 3.11+)
toml==0.10.2  ; python_version < "3.11"
tomli==2.2.1  ; python_version < "3.11"

# Networking & recon
dnspython==2.8.0
ipaddress==1.0.23
netaddr==1.3.0
python-whois==0.9.6
tldextract==5.3.1
psutil==7.2.2
impacket==0.13.1
icmplib==3.0.4
pyasn1==0.6.4
pyasn1-modules==0.4.2
pycryptodome==3.23.0
pyroxy @ git+https://github.com/MatrixTM/PyRoxy.git

# SQLi — retry / parsing
tenacity==9.1.4
sqlparse==0.6.0

# SQLi — database drivers
psycopg2-binary==2.9.12          # PostgreSQL
pymysql==1.1.1                   # MySQL fallback
pymssql==2.4.1                   # MS SQL Server
oracledb==2.5.1                  # Oracle
redis==8.1.0                     # Redis
pymongo==4.18.0                  # MongoDB

# AI
anthropic==0.120.2

# Integrations
python-telegram-bot==22.8
mysql-connector-python==9.7.0

# Utilities
python-dotenv==1.1.1

# Optional — FlareSolverr (Docker, not a pip package):
#   docker run -d --name flaresolverr -p 8191:8191 \
#     -e LOG_LEVEL=info --restart unless-stopped \
#     ghcr.io/flaresolverr/flaresolverr:latest
#   export FLARESOLVERR_URL=http://localhost:8191